# Pretrain Decoder Configuration (v1)
model:
  architecture: decoder_only
  vocab_size: 32768
  hidden_dim: 2048
  num_layers: 24
  num_heads: 16
  max_seq_len: 4096
training:
  dataset: solana_code_and_tx
  batch_size: 8
  learning_rate: 3e-4
  warmup_steps: 2000
  num_epochs: 10
